#version: '2.0'  # Versión de Docker Compose 3.9

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "10000:10000"
    volumes:
      - ./target:/app/target
